apiVersion: v1
kind: 'Secret'
metadata:
  name: '<%= namespace%>-secret-envvars'
  namespace: '<%= namespace%>'
type: 'Opaque'
stringData:<% varsassecret.forEach(function(env){ %>
  <% if (env.value) { %><%= env.key %>: '<%= env.value %>'<% } %>
<% }); %>
---