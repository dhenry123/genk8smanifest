apiVersion: v1
kind: 'Secret'
metadata:
  name: '<%= namespace%>-secret-envvars'
  namespace: '<%= namespace%>'
type: 'Opaque'
stringData:<% global.env.forEach(function(env){ %>
  <%= env.key %>: '<%= env.value %>'<% }); %>
---