apiVersion: 'v1'
kind: 'ConfigMap'
metadata:
  name: '<%= map.name %>'
  namespace: '<%= namespace %>'
data:
  <%= map.filename %>: |+
<% map.content.forEach(line => { %>
    <%- line %>    
<% }) %>
---